{
  "name": "brew",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://jacobdjohnson.github.io/A-W-Brewing/",
  "dependencies": {
    "gh-pages": "^2.1.1",
    "git": "^0.1.5",
    "npm-git-install": "^0.3.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "yarn": "^1.19.1",
    "yarn-install": "^1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
